package org.real_estate_system.io;

import org.real_estate_system.model.Entity;

public class TestEntity extends Entity {
    public TestEntity(String address, double area) {
        super(address, area);
    }
}